name: Build and Deploy Frontend to Azure Web App

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'backend/**'  # Ignora mudanças no backend
      - 'docs/**'     # Ignora mudanças na documentação
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main
  workflow_dispatch:

env:
  NODE_VERSION: '20.x'

jobs:
  build:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}

      - name: Stamp build info (debug)
        run: |
          echo "sha=${GITHUB_SHA}" > dist/__build.txt
          date -u +"built_at=%Y-%m-%dT%H:%M:%SZ" >> dist/__build.txt
          echo "dist contents:"
          ls -la dist | sed -n '1,200p'

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: webapp-dist
          path: dist/

  deploy:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: webapp-dist
          path: dist/

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: dist/

      - name: Smoke check deployed build
        run: |
          echo "Deployed URL: ${{ steps.deploy-to-webapp.outputs.webapp-url }}"
          curl -fsSL "${{ steps.deploy-to-webapp.outputs.webapp-url }}/__build.txt" || true
